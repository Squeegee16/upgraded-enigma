# ============================================================
# Ham Radio App - Python Requirements
# All packages installed at Docker build time as root.
# Plugin installers will skip pip installs at runtime.
# ============================================================

setuptools>=65.0.0
wheel>=0.40.0

# Core Flask framework
Flask==3.0.0
Flask-Login==0.6.3
Flask-WTF==1.2.1
email_validator==1.1.1
Flask-SQLAlchemy==3.1.1
WTForms==3.1.1

# Security
bcrypt==4.1.2
pyopenssl==23.3.0

# Database
SQLAlchemy==2.0.23

# Hardware interfaces
pyserial==3.5
pynmea2==1.18.0

# HTTP and networking
requests==2.31.0
urllib3==2.1.0
websockets==12.0
aiohttp==3.9.1
adif-io==0.6.0

# System monitoring (used by plugins)
psutil==5.9.8

# Image processing (QSSTV, SatDump plugins)
Pillow==10.2.0

# File monitoring (QSSTV, SatDump plugins)
watchdog==4.0.0

# Numerical / DSP
numpy==1.26.4

# DSP filtering (for Morse and P25 plugins)
scipy==1.12.0

# Audio I/O — SoundBlaster Play 3 and other USB cards
# Requires portaudio system library:
#   apt-get install portaudio19-dev libportaudio2
sounddevice==0.4.6

# RTL-SDR Python bindings
# Requires librtlsdr system library (already built
# from source in Dockerfile):
#   RUN apt-get install librtlsdr-dev
pyrtlsdr==0.3
uv
ephem
pyorbital
